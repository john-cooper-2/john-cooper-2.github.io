/* 
1. The two phases to using functions: First we must ___? Next we can execute (or two other words for executing a function?) a function by?
First we must declare the function, Next we execute/call the function.

2. Whats the difference between parameters and arguments PASSED into a function?
Parameters represent arguments, and become arguments when they are passed.

3. Whats the syntax for a named function?
function namedFunc(){}

4. How do we assign a function to a variable?
var namedFunc = function(){}

5.

6. function namedFunc(x, y){
    var name = "noob";
}
console.log (name);

1.)Functions can OPTIONALLY take inputs, and OPTIONALLY return a single value. How do we specify inputs, and how do we specify outputs?
In order to specify inputs and outputs we use parameters and arguments.


























*/